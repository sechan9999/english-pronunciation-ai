# 영어 발음 분석 시스템 환경 설정
# .env.example - 실제 사용 시 .env로 복사하여 사용

# Whisper 모델 설정
WHISPER_MODEL_SIZE=base  # tiny, base, small, medium, large
WHISPER_DEVICE=cpu       # cpu or cuda

# API 서버 설정
API_HOST=0.0.0.0
API_PORT=5000
API_DEBUG=true
API_MAX_WORKERS=4

# 오디오 처리 설정
MAX_AUDIO_LENGTH=60      # 최대 오디오 길이 (초)
AUDIO_SAMPLE_RATE=16000  # 샘플링 레이트 (Hz)
AUDIO_FORMAT=wav         # 기본 오디오 형식

# 분석 설정
ENABLE_PROSODY_ANALYSIS=true
PHONEME_LIBRARY=pronouncing  # pronouncing or pocketsphinx

# 스코어링 가중치
WORD_ACCURACY_WEIGHT=0.6
PHONEME_SIMILARITY_WEIGHT=0.4

# 캐시 설정
ENABLE_MODEL_CACHE=true
CACHE_DIR=./cache

# 로깅 설정
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log

# CORS 설정 (프론트엔드 도메인)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8501

# 보안 설정
SECRET_KEY=your-secret-key-here
RATE_LIMIT=100  # 시간당 요청 제한

# 데이터베이스 (선택, 향후 사용)
# DATABASE_URL=sqlite:///./pronunciation.db

# 외부 API (선택)
# OPENAI_API_KEY=sk-...
# GOOGLE_CLOUD_API_KEY=...
