# 📁 프로젝트 구조 및 아키텍처

## 파일 구조

```
pronunciation-analyzer/
│
├── 📄 pronunciation_analyzer.py    # 핵심 분석 엔진
│   ├── PronunciationAnalyzer 클래스
│   ├── STT (Whisper)
│   ├── 음소 분석
│   ├── 운율 분석
│   └── 피드백 생성
│
├── 🌐 api.py                      # Flask REST API
│   ├── /health                    # 상태 확인
│   ├── /api/analyze               # 전체 분석
│   ├── /api/transcribe            # STT만
│   ├── /api/score                 # 텍스트 스코어링
│   ├── /api/phonemes              # 음소 추출
│   └── /api/practice-sentences    # 연습 문장
│
├── 📱 app.py                      # Streamlit 웹 앱
│   ├── 사용자 인터페이스
│   ├── 오디오 업로드
│   ├── 실시간 피드백
│   └── 학습 통계
│
├── 🧪 test_api.py                 # API 테스트
│   ├── 단위 테스트
│   ├── 통합 테스트
│   └── 클라이언트 예제
│
├── 🎮 demo.py                     # CLI 데모
│   ├── 텍스트 분석 데모
│   ├── 대화형 모드
│   └── API 예제
│
├── 📋 requirements.txt            # Python 의존성
├── 📖 README.md                   # 전체 문서
├── 🚀 QUICKSTART.md              # 빠른 시작
└── ⚙️ .env.example                # 환경 설정 템플릿
```

## 아키텍처 다이어그램

### 시스템 레이어

```
┌─────────────────────────────────────────────────────────────┐
│                    프레젠테이션 레이어                        │
├──────────────────────┬──────────────────────┬───────────────┤
│   Streamlit Web UI   │   Flask REST API     │  Mobile App   │
│   (app.py)           │   (api.py)           │  (Future)     │
└──────────────────────┴──────────────────────┴───────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     비즈니스 로직 레이어                      │
├─────────────────────────────────────────────────────────────┤
│         PronunciationAnalyzer Class                         │
│         (pronunciation_analyzer.py)                         │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ STT Module   │  │ Scoring      │  │ Feedback     │     │
│  │ (Whisper)    │  │ Module       │  │ Generator    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │ Phoneme      │  │ Prosody      │                        │
│  │ Analyzer     │  │ Analyzer     │                        │
│  └──────────────┘  └──────────────┘                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      데이터 레이어                           │
├──────────────────────┬──────────────────────┬───────────────┤
│   Audio Processing   │   Phoneme Dict       │   ML Models   │
│   (librosa)          │   (CMU Dict)         │   (Whisper)   │
└──────────────────────┴──────────────────────┴───────────────┘
```

## 데이터 흐름

### 1. 음성 분석 파이프라인

```
사용자 음성 입력
    │
    ▼
┌─────────────────┐
│ 오디오 전처리    │
│ (librosa)       │
│ • 리샘플링      │
│ • 노이즈 제거   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ STT 변환        │
│ (Whisper)       │
│ • 음성→텍스트   │
│ • 타임스탬프    │
└────────┬────────┘
         │
         ├──────────────┐
         │              │
         ▼              ▼
┌─────────────┐  ┌─────────────┐
│ 발음 분석   │  │ 운율 분석   │
│             │  │             │
│ • 단어 매칭 │  │ • 속도      │
│ • 음소 비교 │  │ • 피치      │
│ • 유사도    │  │ • 에너지    │
└──────┬──────┘  └──────┬──────┘
       │                │
       └────────┬────────┘
                │
                ▼
        ┌───────────────┐
        │ 스코어 계산   │
        │               │
        │ • 가중 평균   │
        │ • 등급 부여   │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │ 피드백 생성   │
        │               │
        │ • 오류 분석   │
        │ • 개선 제안   │
        └───────┬───────┘
                │
                ▼
         사용자에게 반환
```

### 2. API 요청 흐름

```
클라이언트 요청 (HTTP)
    │
    ▼
┌─────────────────┐
│ Flask Router    │
│ (api.py)        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 요청 검증       │
│ • 파라미터 확인 │
│ • 파일 형식    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 임시 파일 저장  │
│ (tempfile)      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Analyzer 호출   │
│ (비즈니스 로직) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 결과 직렬화     │
│ (JSON)          │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ HTTP 응답       │
│ (200/400/500)   │
└─────────────────┘
```

## 핵심 클래스 및 메서드

### PronunciationAnalyzer

```python
class PronunciationAnalyzer:
    """영어 발음 및 유창성 분석 클래스"""
    
    def __init__(self, model_size="base")
        """Whisper 모델 초기화"""
    
    def transcribe_audio(self, audio_path) -> str
        """음성→텍스트 변환"""
    
    def get_phonemes(self, text) -> List[str]
        """텍스트→음소 변환"""
    
    def calculate_pronunciation_score(
        self, 
        reference_text, 
        spoken_text
    ) -> Dict
        """발음 정확도 계산"""
    
    def analyze_prosody(self, audio_path) -> Dict
        """운율 분석 (속도, 피치, 에너지)"""
    
    def generate_feedback(
        self,
        pronunciation_result,
        prosody_result
    ) -> str
        """자연어 피드백 생성"""
    
    def full_analysis(
        self,
        audio_path,
        reference_text
    ) -> Dict
        """전체 파이프라인 실행"""
```

## 스코어링 알고리즘 상세

### 1. 단어 정확도 (Word Accuracy)

```python
correct_words = 0
for i in range(min(len(ref_words), len(spoken_words))):
    if ref_words[i] == spoken_words[i]:
        correct_words += 1

word_accuracy = (correct_words / len(ref_words)) * 100
```

**예시:**
- 참조: "Hello world how are you"
- 인식: "Hello world how you"
- 정확도: 3/5 = 60%

### 2. 음소 유사도 (Phoneme Similarity)

```python
from difflib import SequenceMatcher

ref_phonemes = " ".join(get_phonemes(reference_text))
spoken_phonemes = " ".join(get_phonemes(spoken_text))

similarity = SequenceMatcher(
    None, 
    ref_phonemes, 
    spoken_phonemes
).ratio() * 100
```

**예시:**
- 참조 음소: "HH AH0 L OW1 W ER1 L D"
- 인식 음소: "HH AH0 L OW1 W ER1 L"
- 유사도: 87.5%

### 3. 전체 점수 (Overall Score)

```python
overall_score = (
    word_accuracy * 0.6 +
    phoneme_similarity * 0.4
)
```

**가중치 근거:**
- 단어 정확도 (60%): 의미 전달이 더 중요
- 음소 유사도 (40%): 세밀한 발음 평가

## 성능 최적화

### 1. 모델 캐싱

```python
from functools import lru_cache

@lru_cache(maxsize=1)
def load_whisper_model(model_size):
    return whisper.load_model(model_size)
```

### 2. 배치 처리

```python
def analyze_batch(audio_files, reference_texts):
    with concurrent.futures.ThreadPoolExecutor() as executor:
        futures = [
            executor.submit(
                analyzer.full_analysis,
                audio,
                ref
            )
            for audio, ref in zip(audio_files, reference_texts)
        ]
        return [f.result() for f in futures]
```

### 3. 메모리 관리

```python
import gc
import tempfile

# 임시 파일 자동 삭제
with tempfile.NamedTemporaryFile(delete=True) as tmp:
    # 처리
    pass

# 명시적 가비지 컬렉션
gc.collect()
```

## 확장 가능성

### 단기 개선 (1-2개월)
- [ ] PocketSphinx acoustic scoring 통합
- [ ] 실시간 WebRTC 녹음
- [ ] 사용자 인증 및 세션 관리
- [ ] PostgreSQL 데이터베이스

### 중기 개선 (3-6개월)
- [ ] 모바일 앱 (React Native)
- [ ] 학습 진도 추적
- [ ] AI 튜터 챗봇 (GPT 통합)
- [ ] 다국어 지원

### 장기 비전 (6-12개월)
- [ ] 커스텀 음향 모델
- [ ] 비디오 기반 입모양 분석
- [ ] 소셜 학습 기능
- [ ] 엔터프라이즈 버전

## 기술 스택

| 레이어 | 기술 |
|--------|------|
| **AI/ML** | OpenAI Whisper, Librosa |
| **음소 분석** | CMU Pronouncing Dict, PocketSphinx |
| **백엔드** | Flask, Python 3.10+ |
| **프론트엔드** | Streamlit, React (Future) |
| **오디오** | FFmpeg, librosa, soundfile |
| **배포** | Docker, AWS/GCP (Future) |
| **테스트** | pytest, unittest |

## 코드 품질

### 테스트 커버리지 목표
- [ ] 단위 테스트: 80%+
- [ ] 통합 테스트: 60%+
- [ ] E2E 테스트: 40%+

### 코드 스타일
```bash
# Formatting
black .

# Linting
flake8 .

# Type checking
mypy pronunciation_analyzer.py
```

---

**이 문서는 지속적으로 업데이트됩니다.**
